## Virtual Environment

Create a virtual environment in the project root with `python3 -m venv .venv`.
Activate it with `source .venv/bin/activate` on MacOS/Linux.
Install dependencies with `pip install -r requirements.txt`.
Verify you’re using the venv with `which python` (it should point into `.venv`), and run `deactivate` when you’re done.
